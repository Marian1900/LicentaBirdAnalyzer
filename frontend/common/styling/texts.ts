import { theme } from "./theme";

export const TEXT_STYLES = {
    ERROR_TEXT: {
        color: theme.dangerRed
    },
    LINK: {
        color: "#2358C2",
        fontWeight: "bold" as const
    }
}